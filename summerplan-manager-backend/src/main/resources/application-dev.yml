spring:
  datasource:
    url: jdbc:mysql://localhost:3306/summerplan_manager_dev?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  
  flyway:
    clean-disabled: false

logging:
  level:
    com.summerplan: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE 